kind: pipeline
type: docker
name: default

platform:
  os: windows
  arch: amd64
  # version: "1903"

steps:
  - name: build
    image: mcr.microsoft.com/dotnet/framework/sdk:4.8
    commands:
      - msbuild "WebApplication1\WebApplication1.csproj" /p:Configuration=Release -r:False
